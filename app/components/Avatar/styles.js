import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  profile: {
    height: 65,
    width: 65,
  },
  author: {
    height: 30,
    width: 30,
  },
  image: {
    resizeMode: 'contain',
    width: '100%',
    height: '100%',
  },
});

export default styles;
